using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*Aqui esta la Clase State, lo que hace es que todas las clases herenden de esta*/
public class State : MonoBehaviour
{
    /*

    //Virtual significa usar la versión de la clase extendida, en caso de que tenga, 
    public virtual void InitState() {}

    //Otro método virtual a su vez recibe un parametro Generico "T"
    public virtual void InitState<T>(T param) {}

    public abstract void UpdateState(float delta);

    //Método abstracto, utilizado para clases hijas que tienen herencia, no se puede instancia esta
    //clase con su método
    public abstract void ExitState();

    */
}
