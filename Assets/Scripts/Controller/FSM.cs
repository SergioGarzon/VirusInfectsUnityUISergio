using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FSM : MonoBehaviour
{

    /*
    //Aqui crea una variable denominada estado actual (_currentState)
    protected State currentState;

    

    protected virtual void SwitchState(State newState) {
        
        //En este metodo primero pregunta si el estado que pasamos por parametro es distinto de nulo,
        //tambien se fija si es del mismo tipo, el estado actual y el que le pasamos por parametro
        if(currentState != null && currentState.GetType() == newState.GetType()) {
            //Aqui el return corta el método
            return;
        }
    }*/


}
